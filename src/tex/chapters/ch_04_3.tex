\vspace{-5pt}
\subsection{Loss Function}
\vspace{-3pt}
Kerbl et al.~\cite{kerbl3DGaussianSplatting2023} use a weighted combination of regular $\mathcal{L}_1$ loss and \textit{D-SSIM} (a Structural Similarity Index~\cite{wangImageQualityAssessment2004}):
$L = (1 - \lambda) \cdot \mathcal{L}_1 + \lambda \cdot \mathcal{L}_\text{D-SSIM}$, with $\lambda=0.2$. While $\mathcal{L}_1$ calculates the absolute error as the mean distance between color values of corresponding pixels, D-SSIM among others looks at the features and contrast surrounding these pixels.

\subsection{Backward Pass}
\label{ch:backprop}
During backpropagation the loss from comparing the rendered image resulting from the previous forward pass with the original input image is passed backwards from the image through to 2D Gaussians and finally the 3D Gaussians via gradient descent in principle identical to neural networks~\cite{thimm2025ML}.
If the loss is small enough or the algorithm has reached a maximum number of iterations (usually around 30,000~\cite{kerbl3DGaussianSplatting2023}), backward pass and training algorithm terminate, providing the final reconstructed 3D scene.

\vspace{-8pt}
\subsubsection{Phases}
\vspace{-3pt}
A backward pass is divided into five phases. On the GPU, phases 1--3 use the known $16\times 16$ pixel tiles and thread blocks while phases 4 and 5 are running per-Gaussian.

Also, the algorithm still has access to the per-tile lists of 2D Gaussians sorted by depth as well as the computational graph containing intermediate results from the forward pass.

\paragraph{Phase 1: Loss to Pixel Color Correction} The derivative of $\mathcal{L}_1=\frac{1}{N}\sum_{\text{p=1}}^{N}{\vert{}C_p-I_p\vert{}}$ at a specific pixel $p$ is $\frac{\partial \mathcal{L}_1}{\partial C_p} = \frac{1}{N} \cdot \text{sign}(C_p-I_p)$ with $C$ the rendered color, $I$ the input color.
\begin{flalign*}
\text{Then the full gradient for $L$ is} \hspace{3mm} 
\frac{\partial L}{\partial C_p} = 
\frac{1-\lambda}{N} 
\begin{bmatrix} 
    \text{sign}(R_p - R_{I,p}) \\
    \text{sign}(G_p - G_{I,p}) \\
    \text{sign}(B_p - B_{I,p})
\end{bmatrix}
+ \lambda 
\begin{bmatrix} 
    \frac{\partial \mathcal{L}_{\text{D-SSIM}}}{\partial R_p} \\
    \frac{\partial \mathcal{L}_{\text{D-SSIM}}}{\partial G_p} \\
    \frac{\partial \mathcal{L}_{\text{D-SSIM}}}{\partial B_p}
\end{bmatrix}
\in \mathbb{R}^3. &&
\end{flalign*}
While $\mathcal{L}_1$ provides a direction for color alignment, D-SSIM acts as a structural regularizer.
\subparagraph{Result} The gradients hold the required color correction per pixel. A positive color component gradient indicates an overestimation, vice versa for negative values and $0$ requires no change. The magnitude dictates the degree of adjustment required.\newpage

\paragraph{Phase 2: Pixel Color Correction to 2D Splat} In this phase we need to work alpha compositing backwards ($C_p = \sum_{i=1}^{N} c_i \alpha_i T_i$, with $T_i = \prod_{j=1}^{i-1} (1 - \alpha_j)$, see phase 5 of the forward pass), meaning the $\frac{\partial L}{\partial C_p}$ gradient flows into into the \textit{evaluated} opacity $\alpha_i$ at pixel $p$ and color $c_i$ for each 2D Gaussian in the sorted list for this tile from closest to farthest. 
\begin{flalign*}
\text{Using the chain rule, for color this is} \hspace{0.4cm}
\frac{\partial L}{\partial c_i} = 
\frac{\partial L}{\partial C_p} \cdot \frac{\partial C_p}{\partial c_i} = 
\frac{\partial L}{\partial C_p} \cdot (\alpha_i T_i) 
\in \mathbb{R}^3 &&
\end{flalign*}
\begin{flalign*}
\text{and for evaluated opacity at pixel $p$} \hspace{0.4cm} 
\frac{\partial L}{\partial \alpha_i} = 
\frac{\partial L}{\partial C_p} \cdot \frac{\partial C_p}{\partial \alpha_i} = 
\frac{\partial L}{\partial C_p} \cdot T_i (c_i - C_{behind})
\in \mathbb{R}, &&
\end{flalign*}
with $C_{behind}$ being the accumulated color of everything behind 2D Gaussian $i$. Conceptually, this gradient increases evaluated opacity $\alpha_i$ if the 2D Gaussian's color $c_i$ reduces the pixel error more effectively than the accumulated color of the geometry behind it ($C_{behind}$).
Conversely, if the geometry behind it is more accurate, the gradient decreases $\alpha_i$ to let the background geometry show through.

If the 2D Gaussian spans multiple pixels, the GPU adds all gradients of a type atomically into an aggregated gradient: e.g. $\text{Total } \frac{\partial L}{\partial \alpha_i} = \sum_{p \in \text{Pixels}} \left( \frac{\partial L}{\partial \alpha_i} \text{ for pixel } p \right)$.
\subparagraph{Result} Final gradients for color, intermediate gradients for evaluated opacity.

\paragraph{Phase 3: 2D Splat to 2D Geometry} The gradient for \textit{evaluated} opacity $\alpha_i$ now flows into the physical properties of a 2D Gaussian: the actual base opacity $o$, position $\mu_{2D}$ and 2D covariance matrix $\Sigma_{2D}$ adjusting 2D geometry by moving and changing shape.
For this we recall the opacity distribution $\alpha_i(p) = o \cdot \exp\left(-\frac{1}{2}\Delta p^T \Sigma^{-1}_{2D}\Delta p\right)$ with $\Delta = p - \mu_{2D}$ or simplified with $E$ denoting the exponent: $\alpha_i(p) = o \cdot e^E$.
\begin{flalign*}
\text{For the base opacity, the derivative is simple:} \hspace{3mm}
\frac{\partial L}{\partial o} =
\frac{\partial L}{\partial \alpha_i} \cdot \frac{\partial \alpha_i}{\partial o} =
\frac{\partial L}{\partial \alpha_i} \cdot e^E
\in \mathbb{R}. &&
\end{flalign*}
$e^E$ represents how far the pixel is away from the center of the 2D Gaussian. If the pixel is close to the center, the algorithm increases the base opacity gradient. 

For $\mu_{2D}$ we want to know how the opacity at pixel $p$ changes when we move it:
$$
\text{Let}\hspace{1mm} v = \Sigma_{2D}^{-1} \Delta p\text{:} \hspace{1.5mm}
\frac{\partial \alpha_i}{\partial \mu_{2D}} = o \cdot e^E \cdot \frac{\partial E}{\partial \mu_{2D}} = 
\alpha_i(p) \cdot v
, \hspace{1mm}
\frac{\partial L}{\partial \mu_{2D}} =
\frac{\partial L}{\partial \alpha_i} \cdot \frac{\partial \alpha_i}{\partial \mu_{2D}} =
\frac{\partial L}{\partial \alpha_i} \cdot \alpha_i(p) \cdot v
\in \mathbb{R}^2
$$
Vector $v$ is pointing directly from the 2D Gaussian's center to the pixel. If the evaluated opacity gradient is positive, the center is effectively pulled towards the pixel.

For $\Sigma_{2D}$ we want to know how the opacity at pixel $p$ changes when the shape changes:
$$
\frac{\partial L}{\partial \Sigma_{2D}} = 
\frac{\partial L}{\partial \alpha_i} \cdot \frac{\partial \alpha_i}{\partial \Sigma_{2D}} = 
\frac{\partial L}{\partial \alpha_i} \cdot \frac{1}{2} \alpha_i(p) \cdot \left( v \cdot v^T \right)
\in \mathbb{R}^{2\times 2}
$$
Deriving $\frac{\partial \alpha_i}{\partial \Sigma_{2D}}$ is out of scope for this paper as it is too lengthy. Again we have the vector $v$ multiplied by its own transpose which results in a matrix gradient making the 2D Gaussian stretch itself towards the pixel if the evaluated opacity gradient is positive, or else contract.

Aggregate gradients are calculated like in phase 2.
\subparagraph{Result} Final gradients for base opacity, intermediate gradients for $\Sigma_{2D}$.

\newpage

\paragraph{Phase 4: 2D Geometry to 3D Geometry} The GPU model changes to running per-Gaussian. In this phase the algorithm \textit{unprojects} the 2D Gaussian to the original 3D Gaussian.
We recall the camera projection $\Sigma_{2D} = U \Sigma_{3D} U^T$ with $U = J W_{rot}$, $J$ and $W_{rot}$ as per the forward pass, $t \in \mathbb{R}^3$ the view space position of the 3D Gaussian, $\frac{\partial \mu_{2D}}{\partial t} = J$ and $\frac{\partial t}{\partial \mu_{3D}} = W_{rot}$.
First we unproject from image space to view space using the multi-variable chain rule, then in the same manner from view space to world space, first for $\mu_{2D}$ to $\mu_{3D}$:
\vspace{-14pt}
\begin{flalign*}
\text{View space:} \hspace{6.5mm}
\frac{\partial L}{\partial t} =
\left( \frac{\partial \mu_{2D}}{\partial t} \right)^T \frac{\partial L}{\partial \mu_{2D}} =
J^T \frac{\partial L}{\partial \mu_{2D}}
\in \mathbb{R}^3 &&
\end{flalign*}
\vspace{-12pt}
\begin{flalign*}
\text{World space:} \hspace{5mm}
\frac{\partial L}{\partial \mu_{3D}} =
\left( \frac{\partial t}{\partial \mu_{3D}} \right)^T \frac{\partial L}{\partial t} =
W_{rot}^T \frac{\partial L}{\partial t} =
W_{rot}^T J^T \frac{\partial L}{\partial \mu_{2D}} =
U^T \left( \frac{\partial L}{\partial \mu_{2D}} \right)
\in \mathbb{R}^3 &&
\end{flalign*}
Deriving the same for $\Sigma_{2D}$ to $\Sigma_{3D}$ is out of scope of this paper as it is too lengthy.
\begin{flalign*}
\text{However, the final gradient is very clean:} \hspace{5mm}
\frac{\partial L}{\partial \Sigma_{3D}} = U^T \left( \frac{\partial L}{\partial \Sigma_{2D}} \right) U \in \mathbb{R}^{3\times 3}&&
%\vspace{-8pt}
\end{flalign*}
The unprojections seem intuitive, using the transposes of the forward projection matrices.
\subparagraph{Result} Final gradients for the 3D Gaussian center, intermediate gradients for $\Sigma_{3D}$.

\paragraph{Phase 5: 3D Geometry to 3D Gaussian Attributes} In the last phase the algorithm flows the $\frac{\partial L}{\partial \Sigma_{3D}}$ gradient through the 3D covariance matrix assembly into the raw constituent attributes $s_{raw}$ for the scaling matrix $S$ and quaternion $q_{raw}$ for the rotational matrix~$R$.
The exact math behind is again out of scope, so we list the major steps. $\Sigma_{3D} = R S S^T R^T = M M^T$ with $M = RS$. First we calculate $\frac{\partial L}{\partial M} = 2 \left( \frac{\partial L}{\partial \Sigma_{3D}} \right) M \in \mathbb{R}^{3\times 3}$ and split this into $\frac{\partial L}{\partial S} = R^T \left( \frac{\partial L}{\partial M} \right)$ for scale and $\frac{\partial L}{\partial R} = \left( \frac{\partial L}{\partial M} \right) S$ for rotation.
By extracting the diagonal from $\frac{\partial L}{\partial S}$ we get $\frac{\partial L}{\partial s} \in \mathbb{R}^{+3}$. For the rotation we calculate the partial derivatives of $R$ (chapter~\ref{ch:matrix_decomp}) of each of the four components and aggregate those results into $\frac{\partial L}{\partial q} \in \mathbb{R}^4$.

Finally, we have to backpropagate through activation function $\exp$ for $s_{raw}$, simplifying to $e^{s_{raw}}=s$, and the normalization function for $q_{raw}$ resulting in another Jacobian matrix:
$$
\frac{\partial L}{\partial s_{raw}} = \frac{\partial L}{\partial s} \cdot s
, \hspace{5mm}
\frac{\partial L}{\partial q_{raw}} = J_{norm}^T \frac{\partial L}{\partial q}
\hspace{3mm} \text{with} \hspace{3mm}
J_{norm} = \frac{1}{\vert{}\vert{}q_{raw}\vert{}\vert{}} \left( I - q q^T \right)
$$
\subparagraph{Result} Final gradients for scale and rotation. The backward pass is done.

\subsubsection{Adaptive Density Control (ADC)}
In order to counter over-reconstruction (a 3D Gaussian covering too much geometry) and under-reconstruction (too few 3D Gaussians covering geometry), Kerbl et al. \cite{kerbl3DGaussianSplatting2023} introduced this regulating mechanism which steps in every 100 iterations after the backward pass. For the former case the 3D Gaussian in question is split into two smaller Gaussians, reduced in size by a factor of $1.6$ and positions slightly apart within the space of the old Gaussian. For the latter case the 3D Gaussian is cloned and the clone's position moved towards the positional gradient (see fig. \ref{fig:adc} on page \pageref{fig:adc}). 

New 3D Gaussians simply enter the subsequent forward pass and get adjusted in the pipeline like all other Gaussians.\newpage

Furthermore, there is a pruning step. 3D Gaussians with too low opacity or unbound scale are deleted. In addition to that, every 3,000 iterations the base opacity is reset to almost $0$ for all 3D Gaussians leading to important ones regaining opacity quickly while unimportant ones quickly fade out completely and get deleted.

The authors achieve this by accumulating the 2D positional gradient $\left\Vert{}\frac{\partial L}{\partial \mu_{2D}}\right\Vert{}$. If the sum reaches a threshold when ADC is running, meaning the Gaussian experienced a series of high positional gradients, the algorithm checks if it should split or clone.

ADC did not exist in previous 3D reconstruction technologies and is pivotal for the fast execution times of 3DGS.

\vspace{-5pt}
\subsubsection{Optimization Step}
Where regular gradient descent would simply calculate \texttt{new\_value = old\_value - \linebreak(learning\_rate * gradient)} \cite{thimm2025ML}, 3DGS uses the \textit{Adam} optimizer (Adaptive Moment Estimation) \cite{kingma2015iclr-adam} which keeps a history $t$ on every parameter $p$ and calculates momentum $m_{t,p}$ and variance $v_{t,p}$, weighing the newest gradient against the ones before, e.g. $m_{t,p} = \beta_p m_{t-1,p} + (1 - \beta_p) \cdot new\_gradient$, $\beta_p$ the weight.
With per-parameter learning rate $l_p$, $0 < l_p \ll 1$, the new value makes a tiny step: $l_p \cdot \frac{m_{t,p}}{\sqrt{v_{t,p}} + \epsilon}$. 
Smoothing out over momentum and variance of the past iterations, Adam ensures all 3D Gaussians gently vibrate and morph into their correct geometric shapes, positions, colors and opacities.

Closing the circle to the next forward pass, all new, raw values reside in latent space.
